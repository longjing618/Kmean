This program takes a csv file which contains the coordinates of data points and then outputs the final centroid of clusters.

We can set the number of clusters with variable "numofclusters" in the code.
